# 현재 내용이 없는 Nginx 설정 자리다. 아직 프록시 규칙은 구현되지 않았다.
# TODO: FE 저장소에서 빌드할 Nginx 이미지에 이 파일을 연결한다.
# 목표: 일반 페이지는 frontend:3000, /api 및 필요한 OAuth 경로는 backend:8080.
# SSE는 Spring Boot 응답을 전달하며 해당 경로의 proxy_buffering을 비활성화한다.
# TLS 인증서/개인 키는 이미지에 넣지 않고 실행 시 읽기 전용으로 마운트한다.
# 이 주석만으로는 Nginx가 80/443 포트에서 요청을 받지 않는다.
